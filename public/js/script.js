function clearForm() {
    // Get the form element by its ID
    const form = document.getElementById('registrationForm');
    
    // Reset the form, which clears all input fields and checkboxes
    form.reset();
}
